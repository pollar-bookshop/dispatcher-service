server:
  port: 9003

spring:
  application:
    name: dispatcher-service

  cloud:
    function:
      definition: pack|label # ??? ???? ??? ??? ?? ??

    rabbitmq:
      host: localhost
      port: 5672
      username: user
      pssword: paaword
      connection-timeout: 5s